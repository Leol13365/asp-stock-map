@*@using MultipleModelInOneView;*@
@model List<StockMap.Models.FavoriteViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model[0].StockId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].Stock.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].Stock.FavoriteCount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].StockTrade.Price)
        </th>
        <th>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.Stock.Id, "Details", new { id = item.Id })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stock.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stock.FavoriteCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockTrade.Price)
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
